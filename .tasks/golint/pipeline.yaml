apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: go-lint
spec:
  pipelineSpec:
    resources:
      inputs:
      - name: repo
        type: git
    tasks:
    - name: lint
      taskSpec:
        resources:
          inputs:
          - name: repo
            type: git
        steps:
        - name: lint
          image: cytopia/golint:latest
          args:
          - -set_exit_status
          - ./repo/...
